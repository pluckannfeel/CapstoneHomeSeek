<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="#FFFFFF"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp"
    android:layout_gravity="center"
    >

    <TextView android:textColor="#000000" android:text="Rate Listing" android:id="@+id/rate_dialog_title"
        android:textSize="24dp" android:layout_marginTop="10dp"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <RatingBar
        android:theme="@style/RatingBar"
        android:layout_marginTop="10dp" android:layout_marginBottom="10dp"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:clickable="true"
        android:id="@+id/dialog_ratingbar" android:layout_gravity="center"
        android:numStars="5" android:stepSize="1.0" android:rating="0"
        android:isIndicator="false" />

    <Button android:id="@+id/rate_dialog_button"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/my_button"
        android:text="Rate"  android:focusable="false"
        android:layout_margin="5dip"  android:textColor="#f06c00"
        android:textSize="20dip" android:layout_width="150dp" />

</LinearLayout>